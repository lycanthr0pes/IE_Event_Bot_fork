name: Commitlint

on:
  pull_request:
    branches: [main, develop]

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - run: npm i -g @commitlint/cli @commitlint/config-conventional
      - name: Lint PR title
        env:
          PR_TITLE: ${{ github.event.pull_request.title }}
        run: |
          printf '%s\n' "$PR_TITLE" | commitlint --extends @commitlint/config-conventional
      - name: Lint commits in PR
        if: ${{ !startsWith(github.event.pull_request.head.ref, 'sync/') }}
        run: |
          commitlint \
            --from "${{ github.event.pull_request.base.sha }}" \
            --to   "${{ github.event.pull_request.head.sha }}" \
            --extends @commitlint/config-conventional
      - name: Skip commit lint for sync branches
        if: ${{ startsWith(github.event.pull_request.head.ref, 'sync/') }}
        run: echo "External or generated commits are allowed on sync branches; the PR title is still linted."
